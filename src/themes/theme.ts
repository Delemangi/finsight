import { createTheme } from "@rneui/themed";

export const theme = createTheme({
  lightColors: {
    primary: "#e7e7e8",
    error: "#ff0000",
  },
  darkColors: {
    primary: "#000",
    error: "#ff0000",
  },
  mode: "dark",
});
